<%= form_for(@bob) do |f| %>
  <% if @bob.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bob.errors.count, "error") %> prohibited this bob from being saved:</h2>

      <ul>
      <% @bob.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
